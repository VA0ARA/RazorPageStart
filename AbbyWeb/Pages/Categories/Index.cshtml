@page
@model AbbyWeb.Pages.Categories.IndexModel
@{
}
<h1>list:</h1>
<a class="btn-sm btn-success" asp-page="Create">Create New Category</a>
<table class="table table-hover table-bordered table-responsive w-100">
    <tr>

        <td>Name</td>
        <td>Displayorder</td>
        <td></td>
    </tr>
    @foreach (var obj in Model.categoties)
    {
        <tr>
            <td>@obj.Name</td>
            <td>@obj.DisplayOrder</td>
            <td>
                <div>
                <a  asp-page="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">Edit</a>
                <a  asp-page="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">Delete</a>
                </div>
            </td>
        </tr>
    }
</table>



@*<form asp-controller="Lecturer" asp-action="Delete" asp-route-id="@course.Id" method="post">
    <input type="submit" class="btn-sm btn-danger" value="حذف">
</form>*@